/* eslint-disable no-unused-vars */
import React from 'react';
// Define a React component to render bold text with.
export default function BoldMark(props) {
  return <strong>{props.children}</strong>;
}
